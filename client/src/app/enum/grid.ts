export enum Word {
    Letter,
    Word,
}

export enum TypeCase {
    Word3,
    Word2,
    Letter3,
    Letter2,
    Normal,
    Star,
}

export enum Section {
    UpLeft,
    UpRight,
    DownRight,
    DownLeft,
}

export enum Direction {
    Up,
    Down,
    Left,
    Right,
}

export enum MouseButton {
    Left = 0,
    Middle = 1,
    Right = 2,
    Back = 3,
    Forward = 4,
}
